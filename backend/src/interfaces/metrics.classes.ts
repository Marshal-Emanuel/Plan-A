export interface areGoing{
    areGoing: number;
}

export interface areRemaining{
    areRemaining: number;
    outOf: number;
}

export interface managerMetrics{
    
    totalActiveEvents: number;
    totalPendingEvents: number;
    totalApprovedEvents: number;
    totalRejectedEvents: number;

    totalRSVPs: number;
    totalEvents: number;
    totalEarned: number;
    

}